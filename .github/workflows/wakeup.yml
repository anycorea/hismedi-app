name: Wake up Streamlit

on:
  schedule:
    # 매일 KST 07:00 = UTC 22:00 (전날)
    - cron: "0 22 * * *"
  workflow_dispatch:

jobs:
  wakeup:
    runs-on: ubuntu-latest
    steps:
      - name: Request Streamlit app
        run: |
          curl -I -L --max-time 30 "YOUR_STREAMLIT_APP_URL"
